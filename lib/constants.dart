import 'package:flutter/material.dart';

const Icon dogruIconu = Icon(Icons.mood, color: Colors.green); //kod tekrarlarını önledik
const Icon yanlisIconu = Icon(Icons.mood_bad, color: Colors.red);
